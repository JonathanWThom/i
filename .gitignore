# Rust
/target
Cargo.lock

# Node (husky pre-commit hooks only — see package.json)
node_modules/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code (machine-local; a shared .claude/settings.json stays trackable)
.claude/settings.local.json
.claude/worktrees/
